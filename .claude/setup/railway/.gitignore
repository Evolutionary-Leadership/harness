node_modules/
.env
.env.local
*.log
.DS_Store

# Harness signal file. Written by /mergedev to trigger the auto-merge
# workflow, force-added onto the claude/ branch, and stripped from
# feature/<name> by the workflow's own cleanup before the PR merges.
# It must never reach dev or main.
.pr-description.md
