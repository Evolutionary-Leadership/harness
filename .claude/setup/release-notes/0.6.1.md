### Notes
- Nothing your project receives changes in this release. It moves the harness
  version stamp only, so an upgrade is safe but not urgent.
- This repository is generated. Edits made here are overwritten on the next
  update, so scaffold from it with "Use this template" rather than editing it
  in place.
